module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: [
      // Expo Router integration (correct plugin – not 'expo-router/entry')
      'expo-router/babel',
      // Keep Reanimated plugin last
      'react-native-reanimated/plugin',
    ],
  };
};
